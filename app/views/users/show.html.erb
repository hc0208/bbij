<% provide :title, " | #{@nickname}さん" %>

<div class="my_page contents row">
 <div class = "my_panel panel panel-default">
  <p><%= @nickname %>さんの投稿一般</p>
  <div class ="my_introduce"><%= @introduce %></div>
 </div>
  <%= render :partial => "tweets/tweet", :collection => @tweets %>
  <%= paginate(@tweets) %>
</div>